# sam-claude-config

Central source of truth for global Claude Code configuration. Stores shared instructions and custom skills that stay consistent across all machines.

## Setup

1. Clone the repo
2. Open the repo in Claude Code
3. Run `/claude-global-update` to copy everything to your local `~/.claude/` directory

That's it — every Claude Code session on this machine will now pick up the shared config and skills.
