# Global rules for Claude

## Branch protection

**Never commit directly to `main` or `master`.**

## Commits and pull requests

**Do not commit files or create a pull request unless the relevant skill is invoked.**

- Only commit when told to via `/checkpoint`.
- Only create a pull request when told to via `/final-boss`.
